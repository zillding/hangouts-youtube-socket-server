/* eslint-disable */

var socket = io();

socket.on('init stats', function initStats(msg) {
  // TODO: set initial stats
});

socket.on('update user', function incrementUser(msg) {

});

socket.on('update room', function incrementUser(msg) {

});
